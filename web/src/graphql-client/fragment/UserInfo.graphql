fragment userInfo on User {
	_id
	email
	name
	updatedAt
	createdAt
	password
	profile {
		_id
		comics {
			_id
			name
			description
			thumbnail
		}
		ShortComics {
			_id
			name
			description
			thumbnail
		}
	}
}
