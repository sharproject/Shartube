supergraph:
  # The socket address and port to listen on
  listen: 0.0.0.0:2100
  path: /graphql
  introspection: true

homepage:
  enabled: true

headers:
  all: # Header rules for all subgraphs
    request:
      - propagate:
          matching: .*